-r requirements.txt

# Testing
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2  # requerido por fastapi.testclient.TestClient en tests/

# Calidad de código (Fase 4)
ruff==0.7.4
black==24.10.0
mypy==1.13.0
pre-commit==4.0.1

# Fase 8: stubs de boto3 para mypy --strict (boto3/botocore no publican tipos
# inline) - solo el subconjunto "s3" que usa el proyecto
boto3-stubs[s3]==1.43.75
